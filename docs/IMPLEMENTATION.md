# Build-status record — dictum-binder (`dbind`)

The Verified-rung / implementation-status record owned by Delivery Process (`verified-build-status-tracking`). Separate from the manifest: doc maturity ≠ implementation status. Current status only; history rides on git. Created by slice 1 on 2026-09-18 from `dictum/templates/build-status.template.md`; the *Completes* column is Delivery's extension (STANDARD Part 10e) and is pre-filled for all six rows from the playbook so the union check holds from day one.

Fidelity: `merge` = the seven Quality gates green in `ENV-LOCAL`/`ENV-CI` (nothing substituted; the substitution set is empty); `release` = the release gate at the `v1.0.0` tag.

## Slices

| # | Slice | Type | Realises | Completes (explicit; one completing slice per ID) | Built | Verified (stage) | Proof |
|---|---|---|---|---|:---:|---|---|
| 1 | Foundation — package, LICENSE, console script; Model, Schema (+ generated `dbind.schema.json`), Loader, Emitter, Validator, Renderer, CLI with the catch-all, `errors.py`; `init`, `validate`, `schema`, `--help`, `--version`; golden fixture; fitness suite; CI with the seven gates; `tools/licence_gate.py`, `tools/coverage_report.py`; README sentences; this record | headless, cross-cutting | all `COMPONENT-*`, `ENTITY-*`, `INV-*` (read side), `PATTERN-*`, the five slice-1 `CLI-*`, seven `OUT-*`, eight `ERR-*` (`ERR-SCHEMA-VERSION` partial), all `SEC-*` (write-path three partial), `DEP-RUAMEL-YAML`, `ENV-*`, all `TOOL-*`, five `POLICY-*`, `LEGAL-DICTUM-NAMING`, every `ADR-*`, `SUCCESS-SCHEMA-MATCH` (partial) | `CAP-INIT`, `CAP-VALIDATE`, `CAP-SCHEMA`, `CAP-HELP`, `CAP-PATHCHECK`, `COMPONENT-MODEL`, `COMPONENT-SCHEMA`, `COMPONENT-LOADER`, `COMPONENT-VALIDATOR`, `COMPONENT-RENDERER`, `ENTITY-MAP`, `ENTITY-CONTRACT-ID`, `ENTITY-BINDING`, `ENTITY-LOCATOR`, `ENTITY-FIELD-LOCATOR`, `ENTITY-WIRE`, `ENTITY-ASSERTION`, `ENTITY-PATH`, `ENTITY-COVERAGE`, `ENTITY-COMMENT`, `ENTITY-FINDING`, `INV-ID-GRAMMAR`, `INV-SCHEMA-VERSION`, `INV-CLOSED-KEYS`, `INV-NO-LINE-NUMBERS`, `INV-PATH-FORM`, `INV-SYMBOL-NONEMPTY`, `INV-ROLE-VALUES`, `INV-WIRE-SUBSET`, `INV-ASSERTION-SHAPE`, `INV-FIELD-NAME`, `INV-COVERAGE-WELLFORMED`, `INV-LOCATOR-UNIQUE`, `INV-ASSERTION-UNIQUE`, `INV-COMMENT-ANCHORED`, `INV-COMMENT-TEXT`, `INV-ID-UNIQUE`, `INV-PATH-EXISTS`, `INV-ROLE-REQUIRES-WIRE`, `INV-OWNED-TWICE`, `INV-BYTES`, `PATTERN-ERROR-ENVELOPE`, `PATTERN-EXIT-CODES`, `PATTERN-OUTPUT-MODE`, `CLI-INIT`, `CLI-VALIDATE`, `CLI-SCHEMA`, `CLI-HELP`, `CLI-VERSION`, `OUT-ENVELOPE`, `OUT-ERROR`, `OUT-FINDING`, `OUT-ANCHOR`, `OUT-VALIDATE-RESULT`, `OUT-SCHEMA`, `OUT-INIT-RESULT`, `ERR-USAGE`, `ERR-FILE-MISSING`, `ERR-FILE-EXISTS`, `ERR-FILE-TOO-LARGE`, `ERR-IO`, `ERR-PARSE`, `ERR-INTERNAL`, `SEC-ZERO-NETWORK`, `SEC-ZERO-EXEC`, `SEC-NO-AMBIENT-CONFIG`, `SEC-FAIL-CLOSED`, `SEC-NO-SECRETS`, `DEP-RUAMEL-YAML`, `ENV-LOCAL`, `ENV-CI`, `TOOL-SETUPTOOLS`, `TOOL-ACTIONS-CHECKOUT`, `TOOL-ACTIONS-SETUP-PYTHON`, `POLICY-OUTBOUND-MIT`, `POLICY-CONTRIBUTIONS-MIT`, `POLICY-SOURCE-MARKER`, `POLICY-NAMING-ENFORCEMENT`, `LEGAL-DICTUM-NAMING`, `ADR-SINGLE-FILE`, `ADR-STRUCTURAL-ONLY`, `ADR-NO-LINE-NUMBERS`, `ADR-LOAD-RUAMEL-EMIT-OWN`, `ADR-ARGPARSE`, `ADR-SCHEMA-SINGLE-SOURCE`, `ADR-OWN-SHAPE-VALIDATOR`, `ADR-INIT-REQUIRED`, `ADR-NO-SILENT-DEFAULTS`, `ADR-MAJOR-PER-TEMPLATE`, `ADR-NUMERIC-IDS-REJECTED`, `ADR-NO-LOGGING` | ✅ | release | E2E `tests.e2e.test_journeys` (five journeys + invocation meta-test); unit per INV-* in `tests.unit.test_invariants`; golden `tests.golden.test_canonical`; contract `tests.contract.test_elements`, `tests.contract.test_errors_security`; fitness `tests.fitness.test_structure`, `tests.fitness.test_governance_ops`, `tests.fitness.test_delivery`; seven CI gates green (gate 6 skipped). Proofs owed: `ADR-FORMAT-ONLY-REORDERS` → slice 5; `ERR-USAGE` (--json/mismatch/partial-shape/selector conditions → slice 3, `dbind comment` group → slice 4); `PATTERN-EXIT-CODES` (format --check exception → slice 5); `PATTERN-ERROR-ENVELOPE` (unknown-ID source → slice 2, shape-breaking input → slice 3); `INV-PATH-EXISTS`/`CAP-PATHCHECK` (write-input half → slice 3) |
| 2 | Query — `get`, `list`, `--full` | headless | per the playbook row in `docs/delivery-process.md` | `CAP-QUERY`, `CLI-GET`, `CLI-LIST`, `OUT-BINDING`, `OUT-LOCATOR`, `OUT-FIELD-LOCATOR`, `OUT-ASSERTION`, `OUT-BINDING-SUMMARY`, `ERR-SCHEMA-VERSION`, `SUCCESS-BOUNDED-OUTPUT` | ✅ | release | E2E `tests.e2e.test_journeys` (`test_cap_query`); contract `tests.contract.test_query` incl. the bounded-output test on the generated fifty-binding map; coverage map rows resolved; seven CI gates green (gate 6 skipped). Closes slice 1's owed `ERR-SCHEMA-VERSION` proof for the reader arm; its `set`/`format` forcings stay owed to slices 3 / 5. `ERR-NOT-FOUND` get arm proved here (partial; completes in slice 3) |
| 3 | Writes — `set`, `add-*`, `remove`, `coverage`; the Emitter write path | headless | per the playbook row in `docs/delivery-process.md` | `CAP-SET`, `CAP-ADD`, `CAP-REMOVE`, `CAP-COVERAGE`, `CLI-SET`, `CLI-ADD-LOCATOR`, `CLI-ADD-FIELD`, `CLI-ADD-ASSERTION`, `CLI-REMOVE`, `CLI-COVERAGE-GET`, `CLI-COVERAGE-FULLY-BOUND`, `CLI-COVERAGE-CURATED`, `OUT-WRITE-RESULT`, `OUT-COVERAGE`, `PATTERN-VALIDATE-AROUND-WRITE`, `PATTERN-ATOMIC-REPLACE`, `INV-ORDER-PRESERVED`, `INV-ATOMIC-WRITE`, `ERR-NOT-FOUND`, `ERR-DUPLICATE`, `ERR-INPUT-INVALID`, `ERR-FILE-INVALID`, `SEC-FILE-FOOTPRINT`, `SEC-SYMLINK-FINAL-TARGET`, `SEC-TRUST-BOUNDARY` | ✅ | release | E2E `tests.e2e.test_journeys` (`test_cap_set`, `test_cap_add`, `test_cap_remove`, `test_cap_coverage`); contract `tests.contract.test_writes` (every element, validate-around-write, order-preservation diffs on the canonical and non-canonical fixtures) and the write arms of `tests.contract.test_errors_security` (footprint, symlink chain, mode bits); unit `tests.unit.test_input`; seven CI gates green (gate 6 skipped). Proofs owed: `CLI-SET` comment clause and `ERR-NOT-FOUND`'s `comment get` forcing → slice 4; `ERR-FILE-INVALID`'s `format` forcing → slice 5; `INV-ORDER-PRESERVED` `comment` writes → slice 4; `ERR-INPUT-INVALID`'s `comment set` forcing → slice 4 |
| 4 | Comments — `comment get/set/unset`, comments in the `set` projection | headless | per the playbook row in `docs/delivery-process.md` | `CAP-COMMENT`, `CLI-COMMENT-GET`, `CLI-COMMENT-SET`, `CLI-COMMENT-UNSET`, `OUT-COMMENT`, `COMPONENT-EMITTER` | ✅ | release | E2E `tests.e2e.test_journeys` (`test_cap_comment`, Quality's example 3 with bytes at each step); contract `tests.contract.test_comments` (set/get/unset at all seven anchors with emitter-exact bytes, carrier form follows the text, anchor grammar, value rule, two-carrier refusal, and the `set`-with-comments round trip that closes `CLI-SET`'s owed proof, `ERR-NOT-FOUND`'s `comment get` forcing, `ERR-INPUT-INVALID`'s `comment set` forcing and `INV-ORDER-PRESERVED`'s comment writes); seven CI gates green (gate 6 skipped). Proofs still owed: `ERR-FILE-INVALID`'s `format` forcing → slice 5 |
| 5 | Format — `format`, `format --check` | headless | per the playbook row in `docs/delivery-process.md` | `CAP-FORMAT`, `CLI-FORMAT`, `OUT-FORMAT-RESULT`, `INV-CANONICAL-FIXPOINT`, `ADR-FORMAT-ONLY-REORDERS`, `COMPONENT-COMMANDS`, `COMPONENT-CLI`, `SUCCESS-ROUNDTRIP`, `SUCCESS-COMPLETE-OPS` | ✅ | release | E2E `tests.e2e.test_journeys` (`test_cap_format`); contract `tests.contract.test_format` (exit matrix, --check never writes, only the top-level lists reorder, layout warnings repaired, `ERR-FILE-INVALID`'s `format` forcing incl. `--check-paths`) and `tests.contract.test_complete_ops` (one test per row of Product's operations table); golden `tests.golden.test_canonical` (format twice equals format once over every fixture); fitness `tests.fitness.test_help_tree` (Interfaces acceptance 5 and 10); seven CI gates green (gate 6 skipped). No proofs owed |
| 6 | Release — README install instructions, provenance pass, `bindings.yaml` written by an LLM and conformed with `dbind format`, version `1.0.0`, tag `v1.0.0` | headless (verification-only) | per the playbook row in `docs/delivery-process.md` | `SUCCESS-SCHEMA-MATCH`, `TOOL-RUFF`, `TOOL-PYREFLY`, `POLICY-INBOUND-MIT-ONLY`, `POLICY-PROVENANCE-PASS` | ✅ | release | Release gate run on the tagged commit (checklist below, every item ticked): merge gate green; wheel built with `pip wheel . --no-deps -w dist/`, installed into a fresh venv, E2E tier green against it; README checksum equals `dbind schema --checksum` (`SUCCESS-SCHEMA-MATCH`); `tools/licence_gate.py` green with ruff 0.16.8 and pyrefly 1.3.1 inside their pins (`TOOL-RUFF`, `TOOL-PYREFLY`, `POLICY-INBOUND-MIT-ONLY`, asserted by `tests.fitness.test_governance_ops`); the source-provenance register below (`POLICY-PROVENANCE-PASS`); the repository's own `bindings.yaml` (LLM-written without the tool, conformed with `dbind format`) validates with no errors and `format --check` exits 0 — gate 6 on |

Not completed by any slice, by design: `SUCCESS-CROSS-MODEL` (operator-recorded after the release), `PERSONA-*` (definitions), `CAP-*` (headlines, listed under the slice whose E2E proves them).

## Gate status (summary)

- **Merge gate (`ENV-LOCAL`/`ENV-CI`):** slices 1–6 green on 2026-09-18; re-run green on 2026-09-19 after the executable rename (`lspd` → `dbind`, doc-edit 5c97237, build R1) — unit, golden, contract, fitness, E2E tiers; 100 % line coverage of `src/dbind` via `tools/coverage_report.py`; ruff; pyrefly strict; schema file equals the embedded schema and the README checksum; licence gate MIT-only. Gate 6 on since the release slice: `bindings.yaml` validates with no errors (two deliberate `INV-OWNED-TWICE` warnings, see below) and `format --check` exits 0.
- **Release gate (`v1.0.0`):** run on 2026-09-18, then re-run on 2026-09-19 on the re-cut tagged commit (the tag was moved onto the renamed commit before any publication, per Delivery's re-cut rule); every checklist item ticked.
- **Blocked-by-environment:** none — there is no infrastructure; nothing is deferred.

## Release-gate checklist

- [x] merge gate green on the tagged commit (2026-09-18; re-cut 2026-09-19)
- [x] wheel built with `pip wheel . --no-deps -w dist/` (locally via `uv build --wheel`, the same setuptools backend) and installed into a fresh venv; E2E tier green against it
- [x] README checksum equals `dbind schema --checksum` (`01becb83bc98002f68f5ec5c96d5a8347ccd653ac5708957fc4ce9d600ebf4f0`)
- [x] source-provenance pass recorded below (rows + non-empty residual)
- [x] repository `bindings.yaml` written by an LLM without the tool (a one-off generator script authored for the purpose, from the coverage map and an explicit ID→symbol mapping), validated — zero error-level findings, nothing to fix by hand — then `dbind format` applied; `dbind validate` (also with `--check-paths`) and `dbind format --check` exit 0. Two `INV-OWNED-TWICE` warnings are deliberate: `INV-ATOMIC-WRITE` and `PATTERN-ATOMIC-REPLACE` share the enforcement site `src/dbind/emitter.py#write` (the property and the pattern that guarantees it)
- [x] `pyproject.toml` version `1.0.0` equals the tag `v1.0.0`

## Source-provenance register

Shape owned by Governance & Compliance (`POLICY-PROVENANCE-PASS`); rows filled by the release slice's pass, run on 2026-09-18 by the implementing LLM and re-pointed on 2026-09-19 after the `src/lspd` → `src/dbind` move (a delta pass over the rename diff: string literals and import paths only, no new unit, determinations carried over), module by module over `src/dbind/`, `tests/`, and `tools/`. Determinations: *fresh* = written for this repository from the doc set; *from the spec* = a rule transcribed from a specification's prose, no code copied; *stdlib idiom* = a well-known standard-library usage pattern (the documented API used the documented way), no code copied. No unit in the tree carries a `SOURCE:` marker because nothing was copied, ported, adapted, or transliterated from external code.

| unit (path#symbol) | determination | origin | licence | outbound-compatible |
|---|---|---|---|---|
| `src/dbind/model.py#ID_PATTERN`, `#is_contract_id`, `#kind_of`, `#path_form_problem` | fresh (the ID grammar is Dictum's, tightened per `ADR-NUMERIC-IDS-REJECTED`) | this repository; grammar from the Dictum standard's prose (CC BY 4.0, prose not code) | MIT | yes |
| `src/dbind/model.py#Anchor`, `#Finding`, `#Locator`, `#FieldLocator`, `#Wire`, `#Assertion`, `#Binding`, `#CuratedEntry`, `#Coverage`, `#Map` | fresh (dataclasses mirroring Domain's entities) | this repository | MIT | yes |
| `src/dbind/model.py#SHAPE`, `#_Converter` (all methods), `#from_plain`, `#check_node`, `#binding_to_plain`, `#map_to_plain` | fresh | this repository | MIT | yes |
| `src/dbind/model.py#_split_comment`, `#from_input`, `#_binding_from_input` | fresh | this repository | MIT | yes |
| `src/dbind/schema.py#schema`, `#_spec_schema`, `#_node_schema`, `#schema_json`, `#checksum`, `#check_shape` | fresh; JSON Schema draft 2020-12 keywords used per the specification | this repository; JSON Schema specification (prose) | MIT | yes |
| `src/dbind/emitter.py#needs_quotes`, `#scalar` | from the spec: YAML 1.2 core-schema plain-scalar rules plus ruamel.yaml's round-trip resolver behaviour, verified empirically on 0.19.1; no code copied | YAML 1.2 specification (prose); ruamel.yaml behaviour observed, not copied | MIT | yes |
| `src/dbind/emitter.py#_comment_block`, `#_entry_lines`, `#_flow`, `#_locator_flow`, `#_field_flow`, `#_assertion_flow`, `#_binding_lines`, `#_coverage_lines`, `#emit` | fresh (Domain's Persistence layout, byte for byte) | this repository | MIT | yes |
| `src/dbind/emitter.py#_umask`, `#write` | stdlib idiom: `tempfile.mkstemp` + `fsync` + `os.replace` atomic replace, as documented for `os.replace`/`tempfile`; no code copied | CPython documentation (idiom) | MIT (own code) | yes |
| `src/dbind/loader.py#resolve_target`, `#read_bytes`, `#decode`, `#_line_of`, `#parse`, `#_yaml_reason`, `#_in_scalar`, `#find_comments`, `#_strip_leader`, `#_Anchors`, `#attach_comments`, `#load` | fresh; uses ruamel.yaml's public round-trip API (`YAML(typ="rt")`, `lc`, scanner tokens) the documented way | this repository; ruamel.yaml API (MIT) called, not copied | MIT | yes |
| `src/dbind/validator.py#finalize`, `#_Rules` (all methods), `#validate`, `#validate_input` | fresh (Domain's `INV-*` rows transcribed to checks) | this repository | MIT | yes |
| `src/dbind/render.py#anchor`, `#finding`, `#error`, `#_envelope_object`, `#render`, `#_human`, `#_finding_line` | fresh | this repository | MIT | yes |
| `src/dbind/errors.py` (every class) | fresh | this repository | MIT | yes |
| `src/dbind/cli.py#_HelpAction`, `#_VersionAction`, `#_Formatter`, `#_Parser`, `#_add_global_options`, `#_new_parser`, `#build_parser` | stdlib idiom: `argparse` subclassing (`Action`, `ArgumentParser.error`, `RawDescriptionHelpFormatter`) as documented; no code copied | CPython documentation (idiom) | MIT (own code) | yes |
| `src/dbind/cli.py#parse`, `#_deepest`, `#_check_anchor`, `#_check_assertion_shape`, `#_anchor_of`, `#_json_document`, `#_Run` (all methods), `#main`, `#_fail` | fresh | this repository | MIT | yes |
| `src/dbind/commands/*.py` (every function) | fresh | this repository | MIT | yes |
| `tools/licence_gate.py` (every function) | fresh; reads `importlib.metadata` the documented way | this repository | MIT | yes |
| `tools/coverage_report.py#_IgnoreByFilename`, `#_executable_lines`, `#_excluded_lines`, `#report`, `#main` | fresh; `trace.Trace` and `dis.findlinestarts` used per their documentation; the block-exclusion semantics are modelled on coverage.py's documented behaviour, no code read or copied | this repository; CPython documentation (idiom); coverage.py documented behaviour (Apache-2.0 project, documentation only, no code) | MIT | yes |
| `tests/_helpers.py`, `tests/coverage_map.py`, `tests/unit/*`, `tests/golden/*`, `tests/contract/*`, `tests/fitness/*`, `tests/e2e/*` (every class, method and helper) | fresh; the `sys.addaudithook` footprint spy follows the documented audit-event API | this repository | MIT | yes |
| `tests/fixtures/*.yaml` | fresh, synthetic; `canonical.yaml` is Domain's own Persistence example verbatim (the doc is this repository's) | this repository | MIT | yes |
| `.github/workflows/ci.yml`, `pyproject.toml` | fresh; action pins per their READMEs | this repository | MIT | yes |

**Residual:** the pass ran offline: step (3) of `POLICY-PROVENANCE-PASS` (searching distinctive fragments — identifiers, error strings, regexes — against public sources) could not be executed; resemblance to known code was judged from the implementing model's memory only, which cannot rule out unconscious reproduction of memorised snippets. The units most exposed to that risk are the `argparse` subclassing in `src/dbind/cli.py`, the atomic-replace idiom in `src/dbind/emitter.py#write`, and the tracer replacement in `tools/coverage_report.py`; each is short and idiomatic, but none has been checked against a search. No unit was skipped for size. The residual stands until a networked pass repeats step (3).

## Capability coverage

Built and verified at `merge`: `CAP-INIT`, `CAP-VALIDATE`, `CAP-PATHCHECK`, `CAP-SCHEMA`, `CAP-HELP` (slice 1); `CAP-QUERY` (slice 2); `CAP-SET`, `CAP-ADD`, `CAP-REMOVE`, `CAP-COVERAGE` (slice 3); `CAP-COMMENT` (slice 4); `CAP-FORMAT` (slice 5). Every capability inside the build-ready gate scope is built; slice 6 (Release) is verification-only.

## Notes

- Flake incidents (Quality's policy): none.
- 2026-09-19 — executable rename `lspd` → `dbind` (doc-edit 5c97237, breaking on the CLI surface; package, envelope key, schema file and CI variable followed). Built as one atomic code slice (R1) because no intermediate state passes gate 5 or the help-tree test, then the release re-cut (R2): rows 1 and 6 re-verified at `release`, the `v1.0.0` tag moved onto the renamed commit before any publication. Folded into the existing rows per Delivery's re-cut rule; no seventh row.
